#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    bool all = true;
    cin >> n;
    int p,q;
    cin >> p;
    vector<int> a(p);
    for(int i=0;i<p;i++)
        cin >> a[i];
    cin >> q;
    if((p+q) != 0){

    }
    
}